<button
  type="button"
  class="flex w-full items-center rounded border-b p-1.5
    {{if
      (eq @level 0)
      'gap-2 font-semibold'
      (if (eq @level 1) 'gap-4 font-normal' 'gap-6 font-light')
    }}
    hover:bg-gray-100"
  {{on "click" (if @taskIsVisible @onHide @onShow)}}
>
  {{#if @taskIsVisible}}
    {{svg-jar "eye"}}
  {{else}}
    {{svg-jar "eye-slash"}}
  {{/if}}
  <span class="{{unless @taskIsVisible 'text-gray-500'}}">
    {{yield}}
  </span>
</button>