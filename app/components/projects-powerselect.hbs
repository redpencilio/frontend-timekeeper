<PowerSelect
  class="h-full border-gray-200 -rounded shadow-sm focus:ring-indigo-500 focus:border-indigo-500"
  @searchEnabled={{true}}
  @options={{this.projects}}
  @selected={{this.selectedProject}}
  @searchField="name"
  @onChange={{this.onChange}}
  as |project|
>
  <div class="flex items-center text-sm h-8">
    <span
      class={{concat "inline-block w-3 h-3 rounded-full mr-3 " project.color}}
    ></span>
    <span class="text-gray-700">{{project.name}}</span>
  </div>
</PowerSelect>