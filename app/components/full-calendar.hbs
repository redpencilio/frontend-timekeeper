{{#if this.selectedRange}}
  <TimeLogPopover
    @onSave={{this.onEventsAdded}}
    @onCancel={{this.clearSelectedRange}}
  />
{{/if}}
<div class="full-calendar h-full" {{did-insert this.setupCalendar}}></div>